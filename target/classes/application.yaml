spring:
  datasource:
    url: jdbc:h2:mem:testdb
    initialization-mode: always
  #    schema: schema.sql
#    data: data.sql

  h2:
    console:
      enabled: true
      settings:
        trace: true
      path: /h2-console

  jpa:
    hibernate:
      ddl-auto: create-drop